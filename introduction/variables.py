# Text Type:	str
# Numeric Types:	int, float, complex
# Sequence Types:	list, tuple, range
# Mapping Type:	dict
# Set Types:	set, frozenset
# Boolean Type:	bool
# Binary Types:	bytes, bytearray, memoryview


# STR
charcter = 'N'
charcter = '1'
charcter = '#'
sentence = 'Hello from here'
sentence = '1 + 2'

# Numbers
number = 1
number = 1216581
number = 1.00008
number = 5+10

# Sequence Types
# Lists
l = list()
l = []
l = [1, 2, 3]
# Lists are not type bound
l = [1, 'Fedy', 5+10]

# Tuples
# are type bound
t = (1, 2, 3)
t = ('Fedy', 'Walid', 'Python')

# Common operations on lists tuples and strings
s = 'This is python variables'
l = [1, 'Fedy', 15]
t = ('Fedy', 'Walid', 'Python')

firstLetter = s[0]
lastLetter = s[-1]

firstElementOfList = l[0]
lastElementOfList = l[-1]

firstElementOfTuple = t[0]
lastElementOfTuple = t[-1]

# for element in t:
#     print(element)

# helper functions
table = s.split()
print(table)

# Booleans
b = True
b = False

b = (1 == 1)
b = (1 != 1)
b = (1 > 1)
b = (1 < 2)

# And
# True & False = False
# intere -> inter -> bulb
b = (1 < 2) & ('i' == 'I')

# Or
# True | False = True
# intere
#         => bulb
# intere
b = (1 < 2) | ('i' == 'I')
print(b)

# DICT
di = {
    'name': 'fedy',
    'age': 28,
}
di = {
    'identification': 'value',
    'identification': 'value',
    'identification': 'value',
}
